# Minimal exporter-level config (auth_modules, etc.).
# Empty on purpose: we connect via DATA_SOURCE_NAME and only need the custom
# queries in queries.yaml. Without this file, postgres_exporter v0.15 logs a
# (non-fatal) warning on every start: "Error opening config file
# \"postgres_exporter.yml\": no such file or directory".
auth_modules: {}
