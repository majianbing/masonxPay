apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

resources:
  - deployment.yaml
  - service.yaml

patches:
  - path: patch-gateway-config.yaml
    target:
      kind: ConfigMap
      name: masonxpay-config
