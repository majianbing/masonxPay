fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
qdrant-client==1.12.1
# Optional: only needed when RAG_MODEL_PROVIDER=openai (external answer generation).
openai==1.59.6
# Optional: only needed when RAG_VECTOR_BACKEND=llamaindex (RAG5 framework bakeoff).
llama-index-core==0.12.52.post1
