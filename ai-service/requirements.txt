fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
qdrant-client==1.12.1
# Optional: only needed when RAG_MODEL_PROVIDER=openai (external answer generation).
openai==1.59.6
# The RAG5 llamaindex bakeoff backend needs extra deps that bump pydantic; they are
# kept out of this image. See requirements-bakeoff.txt for local bakeoff runs.
